# Client-Request-Desk
A workspace-aware customer request management system with React, Node.js, and SQLite. Manages request lifecycle (NEW → QUALIFIED → CLOSED), prevents duplicate conversions, and enforces complete data isolation between workspaces.
